{{#each layouts}}
  {{#each layout.items}}
${{meta.name}}-name: '{{meta.name}}'
${{meta.name}}-x: {{x}}px
${{meta.name}}-y: {{y}}px
${{meta.name}}-offset_x: -{{x}}px
${{meta.name}}-offset_y: -{{y}}px
${{meta.name}}-width: {{width}}
${{meta.name}}-height: {{height}}
${{meta.name}}-total_width: {{width}}
${{meta.name}}-total_height: {{height}}
${{meta.name}}-image: '{{classname }}'
${{meta.name}}: ({{x}}px, {{y}}px, -{{x}}px, -{{y}}px, {{width}}, {{height}}, {{total_width}}, {{total_height}}, '{{{url}}}', '{{meta.name}}', )
  {{/each}}
{{/each}}

{{#each layouts}}
  {{#each sprites}}
  	{{#if dpi}}
			{{escimage url}}
		{{/if}}
  {{/each}}
{{/each}}
